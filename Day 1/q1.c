//Write a program to input two numbers and display their sum.

#include <stdio.h> 
int main() {
    int x, y, z;

     scanf("%d" "%d", &x,&y);

    z = x + y;

    printf("The sum of %d and %d is %d\n", x, y, z);
    return 0;
} 